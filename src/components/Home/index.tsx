import React, { ReactElement } from "react";
import GameList from "../GameList";

const Home = (): ReactElement => {
  return (
    <main>
      <GameList />
    </main>
  );
};

export default Home;
